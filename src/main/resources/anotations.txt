
Proximas mudancas:





0 - 
1 - Criar funcao na qual a ia gere as questoes mais frequentes apartir de uma pontacao de frequencia
2 - dar opcao para escolher a quantidade de questoe que quero fazer e questoes ja feitas tirar da lista ate que seja restaurado manualmente ou concluido todas as questoes economizando
permitir refazer apenas as questoes incorretas um determinado numero de vezes espacadamente e as corretas tambem, sendo que as erradas em tempos curtos e as respondidas corretamente e m um tempo maior como no anki
2 - nas questoes de estudo reverso incluir funcao para que explique os termos apresentados e significados usando ia
2 - corrigir o upload de arquivos que nao esta funcionando









======================================================


 CONCLUIDAS:
   0 - no projeto java adicionar funcionalidade que permita processar a tabela de questoes com as questoes e respostas para a pipeline da base de conhecimento















1 - ajustar para que ia crie  versione  uma mesma questao em diferentes formas mudando as palavras mas sem perder o sentido
para se aprender a logica da questao.



 6 - manter historico dos resultados para acompanhamento
 7 - Salvar os esqueletos gerados no estruturador de Discursiva
 8 - painel administrativo tela de gestao IA as questoes geradas por IA nao aparecem, adicionar funcioonalidade para gerar questoes nessa tela

concluidos:
    1 - Corrigir resultado do simulado que nao esta calculo e mostrando os resultados
    2 - upload de arquivos em drag and drop e divisao de arquivos por tamanho.







  -----------------------------------\
  a - Análise de Prioridades (Pareto 80/20) aparentemente nao esta calulando corrigir como ultima prioridade
  b - 1 - Pipeline de Limpeza de Dados (Data Cleaning):
        Automação: O sistema deve aplicar algoritmos pré-programados de limpeza (ex: remoção de stop-words, normalização de caracteres, remoção de duplicatas).
        Inteligência: Implementar lógica para o sistema sugerir/aplicar a melhor técnica de limpeza baseada no tipo de arquivo.
        Fallback Manual: Caso o sistema identifique baixa confiança na limpeza automática, deve abrir uma Interface de Edição para o usuário realizar ajustes manuais nos dados antes de prosseguir.

######################   REVISAO APRA ESTRUTURA DE RAG #####################################


Minha implementação atual está instável e lenta nas respostas. Preciso revisar e refatorar o pipeline de dados para ter certeza que segue este fluxo lógico rigoroso:
1. Camada de Embedding:
Use a biblioteca ollama para gerar embeddings. Garanta que o modelo de embedding (ex: nomic-embed-text) seja consistente tanto na ingestão quanto na busca.
2. Implementação do Cache Semântico:
Antes de chamar o LLM (ollama), o sistema deve converter a pergunta  em vetor.
Execute uma busca na tabela cache_questoes usando o operador de distância de cosseno (<=>).
Se a distância for menor que 0.1 (muito similar), retorne a resposta do banco imediatamente e interrompa o processo (economizando processamento).
3. Implementação do RAG (se o cache falhar):
Busque os top 3 fragmentos mais relevantes na tabela conhecimento_estudo.
Use uma consulta SQL que combine a similaridade vetorial com filtros (ex: filtrar pela matéria específica do simulado ou topicos, etc...).
4. Engenharia de Prompt (Ollama):
certique-se  que toda a parte do sistema que precise fazer consultas e obter respostas por ia  siga essa logica .
5. Fechamento do Ciclo:
sempre que Após o Ollama/IA  gerar a questão, o sistema deve salvar automaticamente a Pergunta, a Resposta e o Vetor na tabela de cache_questoes.
6 - ajuste na melhor parte possivel do painel de admin um painel para que eu possa fazer RAG para melhorar a base de ifnormacoes da IA, modifique o que for necessario e me mostre como usar

========================================================================================================
========================================================================================================




Você é um engenheiro sênior de backend, frontend e DevOps.

Sua tarefa é gerar o código completo de um sistema de Text-to-Speech (TTS) usando Kokoro TTS,
 pronto para rodar com Docker e docker-compose e integrar ao projeto atual de simulador de questões
  e com dois frontends: um simples (para ouvir as respostas das questoes) e um avançado (painel de controle de testes para
  integracoes com o modelo de ia e endpoints do servico de text-to-speech).

---

## Contexto do sistema existente

## Objetivo

Adicionar:

* geração de áudio das respostas
* streaming de áudio em tempo real
* botão "ouvir resposta"
* painel avançado para testes e configuração de voz
* integração com IA local (ex: Ollama)

---

## 1. Serviço TTS (Kokoro)

* Usar Kokoro TTS
* Suporte a português (pt-BR)
* Suportar:

  * geração completa (.wav)
  * streaming em tempo real

---

## 2. API (FastAPI)

Endpoints:

### POST /tts

* entrada: texto
* saída: wav completo

### POST /stream

* entrada: texto
* saída: streaming de áudio

### POST /config

* configurar:

  * voz (masculina/feminina)
  * ritmo (via texto)
  * pausas
  * entonação

### POST /ai

* integração com IA local (Ollama)
* entrada: prompt
* saída: texto gerado + áudio (streaming opcional)

---

## 3. Integração com simulador

Criar exemplo:

GET /questions/{id}

Resposta:

{
"question": "...",
"answer": "...",
"audio_url": "...",
"audio_stream_url": "..."
}

---

## 4. Frontend simples (usuário)

usar o frontend atual das paginas de respostas do simulado e estudo reverso com

* botão: 🔊 Ouvir resposta
*  se o texto ja existir no banco de cache
  * chamar streaming
   e permitir com click  tocar áudio resposta
   se o texto resposta nao existir e se for usado ia para dar resposta permitir ouvir a resposta ao vivo enquanto a ia responde ao mesmo
   tempo que responde o texto.

---

## 5. Frontend avançado (painel separado)

integrar um painel separado integracado ao design do sistema atual

### Funcionalidades:

* campo para digitar texto
* botão "Gerar áudio"
* player de áudio
* botão "Streaming em tempo real"
* permitir ouvir as respostas da base de conhecimento em audio

### Configurações em tempo real:

* selecionar voz
* ajustar ritmo (simulado)
* adicionar pausas
* editar texto com preview

### Integração com IA:

* campo de prompt
* botão "Gerar com IA"
* exibir resposta
* botão "Ouvir resposta da IA"
permitir desabilitar a integracao(caso ocorra problema de desenpenho)

### Debug:

* logs de requisição
* tempo de resposta
* tamanho do áudio

---

## 6. Streaming

* usar HTTP chunked ou WebSocket
* áudio começa antes de terminar
* sem bloqueio

---

## 7. Configuração

* JSON em memória
* atualizável em runtime
* aplicado imediatamente

---

## 8. Docker

Gerar:

* Dockerfile
* docker-compose.yml

Serviços:

* tts-service
* opcional: mock do simulador
* opcional: ollama (comentado ou configurável)

---

## 9. Estrutura

Organizar logicamente:

* API
* serviço TTS
* config
* integração IA
* frontend simples
* frontend avançado

---

## 10. Requisitos técnicos

* Python 3.11+
* FastAPI
* código assíncrono
* baixa latência
* logging
* tratamento de erros

---

## 11. Importante

* NÃO gerar múltiplos arquivos separados
* retornar tudo em TEXTO CONTÍNUO
* separar com comentários:

# Dockerfile

# docker-compose.yml

# app/main.py

# frontend/simple.html

# frontend/admin.html

---

## 12. Extras importantes

* streaming fluido
* suporte a textos longos
* dividir texto em chunks
* evitar travamentos

---

## Formato da resposta

Retorne apenas o código completo, organizado por seções com comentários.
Não explique nada.
